
const consumerPlans = [
    {} // Empty instance to represent a unlimited consumer plan with -1 quota (unlimited).
];

const developerPlans = [
    {} // Empty instance to represent a unlimited consumer plan with -1 quota (unlimited).
];

module.exports = {
    consumerPlans,
    developerPlans,
};
